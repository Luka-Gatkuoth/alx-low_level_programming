#include"main.h"

/**
* mul- A function that multiplies two integers
* @x: The first number to be multiply
* @y: The second number to be multiply
* Return: 0 (success)
*/

int mul(int x, int y)
{
return (x * y);
}
